<%= form_for(@login_module) do |f| %>
  <% if @login_module.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@login_module.errors.count, "error") %> prohibited this login_module from being saved:</h2>

      <ul>
      <% @login_module.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
