<%= form_for(@predator) do |f| %>
  <% if @predator.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@predator.errors.count, "error") %> prohibited this predator from being saved:</h2>

      <ul>
      <% @predator.errors.full_messages.each do |msg| %>
        <li><%= msg %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :name %><br>
    <%= f.text_field :name %>
  </div>
  <div class="field">
    <%= f.label :teeth_size %><br>
    <%= f.text_field :teeth_size %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
